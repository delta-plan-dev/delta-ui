{
  "name": "delta-ui",
  "version": "0.1.0",
  "private": false,
  "babel": {
    "presets": [
      "@babel/preset-react"
    ]
  },
  "scripts": {
    "publish:npm": "rm -rf dist && mkdir dist && babel ./src/delta-ui -d dist --copy-files"
  },
  "devDependencies": {
    "@babel/cli": "^7.12.8",
    "@babel/preset-react": "^7.12.7",
    "@types/react": "^16.9.38",
    "@types/styled-components": "^5.1.0",
    "typescript": "^3.7.5"
  },
  "dependencies": {
    "react": "^16.13.1",
    "styled-components": "^5.1.1"
  }
}
